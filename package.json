{
  "name": "siyu-creativity",
  "version": "0.1.0",
  "private": true,
  "description": "Premium catalogue website, admin CMS, and Express API for Siyu Creativity.",
  "workspaces": [
    "client",
    "admin",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspace client",
    "dev:client": "npm run dev --workspace client",
    "dev:admin": "npm run dev --workspace admin",
    "dev:server": "npm run dev --workspace server",
    "build": "npm run build --workspace client && npm run build --workspace admin && npm run build --workspace server",
    "lint": "npm run lint --workspace client && npm run lint --workspace admin && npm run lint --workspace server",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
